=> For the website files either start a local server and drag and drop the folder or files to open the site or after unzipping the file double click on index.html file.

=> For the Python Programming files first install all the libraries added in the files. To install Python libraries open CMD and type  pip install library_name , if Anaconda is used
   then conda install library_name.
